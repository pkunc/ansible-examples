---
all:
  hosts:
  children:
    demo_servers:
      hosts:
        demo_server:
          ansible_host: server.example.com
          ansible_user: centos
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
